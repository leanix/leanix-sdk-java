mutation {
  updateFactSheet(id: "%s", 
    rev: %d, 
    validateOnly: false, 
    patches: {
      op: add, 
      path: "/relToChild/new", 
      value: "%s"}) {
    factSheet {
      id
      rev
    }
  }
}