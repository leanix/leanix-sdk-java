mutation {
  updateFactSheet(id: "%s", 
    rev: %d, 
    validateOnly: false, 
    patches: {
      op: add, 
      path: "/relToParent/new", 
      value: "%s"}) {
    factSheet {
      id
      rev
    }
  }
}